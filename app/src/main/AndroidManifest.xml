<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.picturesharing">
    <!-- 允许程序打开网络套接字(Allowsapplications to open network sockets) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--        android:name2="com.example.data.OldTeacherData"-->
    <!--        android:name3="com.example.data.RecentTeacherData"-->
    <!--        android:name4="com.example.data.RecentTeacherData"-->
    <application
        android:name="com.example.dao.User"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PictureSharing"
        tools:targetApi="31">

<!--         <activity-->
<!--         android:name="."-->
<!--         android:exported="true" />-->



        <activity
            android:name=".BottomBarActivity"
            android:exported="false" />

        <activity
            android:name=".DetailCourseActivity"
            android:exported="false"/>
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>